# 设计验收报告

## 对照对象

- 主页外观基准：`public/product-concept-thin-touch-standby.png`，1672 × 941 像素。
- 四区触控正面 CAD：`public/cad-touch-front-target.svg`，由参数化实体生成，110 条可审计路径。
- 四区触控装配 CAD：`public/cad-touch-assembly-target.svg`，由同一实体爆炸投影生成，494 条可审计路径。
- 参数与放行边界：`public/cad-touch-enclosure-target.validation.json`。
- 本轮使用同一张并排对照图检查主页外观、正面 CAD 和装配 CAD；验收后已删除临时截图。

## 目标结构

- 标准 86 × 86 毫米面板，正面 2.6 毫米目标厚度，5 毫米圆角。
- 四块独立、等宽、纵向齐平触控面，每块宽 19.9108 毫米，间隙 0.72 毫米。
- 四个灯窗和一个麦克风孔的位置、数量与主页外观表达一致。
- 装配层级明确表达：正面触控面、四区触控电极占位、PCB 机械包络占位、载架和后壳。

## 本轮发现与修复

- `[P1]` 原工程 CAD 仍是四个小机械按键，与主页的四块大面积齐平触控面不一致。已重新建立参数化四区触控外壳，正面工程图与主页分区方式一致。
- `[P1]` 原装配图层件重叠、装配关系不清。已改为真实实体的爆炸装配投影，分别显示面板、触控电极占位、PCB 占位、载架和后壳。
- `[P1]` 仅展示线框图无法证明工程资产可继续使用。已提供 STEP、STL、DXF 和结构化验证 JSON 下载。
- `[P1]` 当前 PCB 尚未按新外壳重构。页面和验证文件均明确标记 `pcb_adapted=false`，PCB 工程图继续保留为当前已保存状态，不将占位体冒充真实适配结果。

## 必查五项

- 字体与排版：延续现有宋体标题、苹方正文和黑金文化感；新增下载入口使用既有细线框语言。
- 间距与布局：工程卡片层级未改；正面与装配图在原卡片内完整显示，无裁切、遮挡或文字溢出。
- 色彩与视觉变量：CAD 主线使用克制金色，隐藏线使用低对比灰绿色，与现有网站一致。
- 图片质量与资产忠实度：主页图、正面 CAD 和装配 CAD 已放在同一对照图中检查；四区分割、灯窗数量、圆角和整体比例一致。
- 文案与内容：全部中文；明确区分主页概念图、真实参数化 CAD、当前 PCB 保存态和未来 PCB 适配。

## 功能与工程检查

- 参数化验证：86 × 86、四区独立、四区等宽、四个灯窗、正面实体有效、装配实体有效，全部通过。
- 文件完整性：SVG、STEP、STL、DXF、JSON 本地 HTTP 均返回 200；STEP 565907 字节，STL 264084 字节。
- `npm test`：7/7 通过（Node 24 工作区运行时）。
- `npm run lint`：0 错误；保留 4 条项目既有 `<img>` 性能提示。
- 浏览器实测：正面 CAD 与装配 CAD 均可点击打开高清弹窗；缩放从 100% 到 125% 成功；关闭弹窗成功；下载入口可访问。

## 放行边界

- 当前结果是 `DIGITAL_DESIGN_CANDIDATE_MEASUREMENT_GATED`，不是量产外壳、模具放行或市电样机放行。
- PCB 仅使用 82 × 82 × 1.6 毫米机械包络占位。更换实际 PCB 后，必须重新执行碰撞、端子、声学、热、爬电电气间隙和实物装配验证。

final result: passed
